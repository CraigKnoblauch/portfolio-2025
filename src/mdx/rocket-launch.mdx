import { useRef } from 'react'
import { Canvas } from '@react-three/fiber'
import { OrbitControls } from '@react-three/drei'
import { RocketLaunchModel } from 'src/components/RocketLaunchModel.jsx'

Here's some content as an example

<Canvas>
    <ambientLight />
    <perspectiveCamera position={[0, 0, 5]} />
    <OrbitControls />
    <RocketLaunchModel />
</Canvas>

That's all for now. I'll work on the other stuff in a bit

This is **bold**

This is _italic_ and this is bold **_italic_**

```
this is a code block
```